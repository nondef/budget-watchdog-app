<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <!-- Theme.AppCompat.DayNight varsayılanı light'ta #fafafa, dark'ta
             #303030; ikisi de sayfa zeminiyle uyumsuz. WebView'ın altında
             kalan anlarda (klavye inset'i yeniden hesaplanırken, splash
             geçişinde) bu renk göründüğü için sayfa zemini tonlarına
             çekildi. -->
        <item name="android:windowBackground">@color/windowBackground</item>
    </style>


    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">@drawable/splash</item>
        <!-- bkz. AppTheme.NoActionBar. Splash görseli `android:background`
             ile çizildiği için bu değer splash'i etkilemez. -->
        <item name="android:windowBackground">@color/windowBackground</item>
    </style>
</resources>
